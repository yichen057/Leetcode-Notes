# 🌟 Day 5 LeetCode 笔记：哈希表专题01（242 / 349 / 202 / 1）

---

## 📘 一、哈希表理论基础

在做面试题时，只要出现 **“判断一个元素是否出现过”** 的场景，
就应该第一时间想到 **哈希法（Hash Table）**。

### 🔑 什么时候用哈希表？

当我们需要：

- 快速判断一个元素是否出现；
- 统计每个元素出现的次数；
- 在 O(1) 时间内查找、插入或删除元素。

这类问题都可以考虑用哈希表结构。

### 📊 常见哈希结构

| 数据结构 | 说明 | Python 实现 |
|-----------|------|-------------|
| 哈希表（Hash Table） | 存储键值对映射关系 | `dict` |
| 哈希集合（Hash Set） | 只存 key，不存 value，用于去重或存在性判断 | `set` |

### 💡 重要结论

> “当你遇到需要快速判断一个元素是否出现在集合中时，就要考虑 **哈希法**。”

📚 延伸阅读：[程序员Carl - 哈希表理论基础](https://programmercarl.com/哈希表理论基础.html)

---

## 🧮 二、题目一：1. Two Sum（两数之和）

### 🔍 题目描述

给定一个整数数组 `nums` 和一个整数目标值 `target`，请返回使两数之和等于 `target` 的两个数的下标。

---

### 💡 思路讲解：哈希表解法

当我们遍历到某个元素 `a` 时，想判断 `(target - a)` 是否已经出现过。  
如果要快速判断一个元素是否出现过，就应使用哈希表。

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        hashmap = {}
        for i, num in enumerate(nums):        # i是索引, num是当前数
            if target - num in hashmap:       # 查找是否已有对应数
                return [hashmap[target - num], i]
            hashmap[num] = i                  # 存储当前数的索引
        return []
```

### 🧠 核心理解点

1. **为什么使用哈希表？**  
   因为需要快速判断 `(target - num)` 是否出现过。

2. **为什么用字典？**  
   存储 `(数值 → 索引)` 映射。

3. **为什么不用双指针？**  
   双指针需排序，会改变下标，无法返回原始索引。

4. **时间复杂度**：O(n)  
   **空间复杂度**：O(n)

5. **技巧**：`enumerate(nums)` 可以同时获取索引和值，比 `range(len(nums))` 更简洁。

6.    for j in range(i + 1, len(nums)): # i + 1 表示循环的起始索引（也就是“从 i 的下一个位置开始”). 

      range(start, stop), 表示生成一个从 start 到 stop - 1 的整数序列。


---

### 🧩 暴力法（for 双层循环）

```python
class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for j in range(i + 1, len(nums)):  # i + 1 表示从下一个索引开始
                if nums[i] + nums[j] == target:
                    return [i, j]
        return []
```

- 时间复杂度：O(n²)  
- 空间复杂度：O(1)

---

## 🔠 三、题目二：242. Valid Anagram（有效的字母异位词）

### 💡 思路讲解

判断两个字符串是否由相同字母组成，顺序可以不同。

---

### ✅ 方法一：排序法

```python
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        return sorted(s) == sorted(t)
```

- 时间复杂度：O(n log n)
- 空间复杂度：O(1)

---

### ✅ 方法二：哈希表计数法

```python
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        if len(s) != len(t):
            return False
        count = {}
        for ch in s:
            count[ch] = count.get(ch, 0) + 1
        for ch in t:
            if ch not in count:
                return False
            count[ch] -= 1
            if count[ch] < 0:
                return False
        return True
```

- 时间复杂度：O(n)  
- 空间复杂度：O(1)（常数字母集）

---

## 🔢 四、题目三：349. Intersection of Two Arrays（两个数组的交集）

### 💡 思路讲解

用 `set` 去重并求交集。

```python
class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        set1 = set(nums1)
        set2 = set(nums2)
        return list(set1 & set2)  # & 表示交集
```

- 时间复杂度：O(n + m)  
- 空间复杂度：O(n + m)

---

## 😊 五、题目四：202. Happy Number（快乐数）

### 💡 思路讲解

不断将数字替换为「各位数字的平方和」，判断最终是否能变为 1。  
若陷入循环，则不是快乐数。

---

### ✅ 方法一：集合判环法

```python
class Solution:
    def isHappy(self, n: int) -> bool:
        seen = set()
        while n != 1:
            if n in seen:        # 出现过，说明形成循环
                return False
            seen.add(n)
            n = sum(int(i)**2 for i in str(n))
        return True
```

---

### ✅ 方法二：快慢指针法（Floyd 判环算法）

```python
class Solution:
    def isHappy(self, n: int) -> bool:
        def next_number(num):
            return sum(int(i)**2 for i in str(num))

        slow = n
        fast = next_number(n)
        while fast != 1 and slow != fast:
            slow = next_number(slow)
            fast = next_number(next_number(fast))
        return fast == 1
```

#### 🌈 思维理解
- 如果 `fast == 1`：快乐数 ✅  
- 如果 `slow == fast`：陷入循环 ❌

#### ⏱️ 复杂度
- 时间复杂度：O(log n)
- 空间复杂度：O(1)

---

## 🧾 六、Day 5 总结表

| 题号 | 名称 | 思路 | 数据结构 | 时间复杂度 | 空间复杂度 |
|------|------|--------|-----------|--------------|--------------|
| 1 | Two Sum | 哈希表查找 | dict | O(n) | O(n) |
| 242 | Valid Anagram | 哈希计数 | dict | O(n) | O(1) |
| 349 | Intersection of Two Arrays | 集合交集 | set | O(n+m) | O(n+m) |
| 202 | Happy Number | 判环 / 快慢指针 | set / 指针 | O(log n) | O(1) |

---

## 🪄 七、学习要点回顾

- 当题目出现 **“是否出现过”**、**“快速查找”**、**“去重”** 时 → 想到哈希。
- `dict` 用于映射；`set` 用于去重。
- `enumerate()` 可以同时获取索引和值。
- `range(i + 1, len(nums))` 表示从第 i+1 个位置开始循环。

---

## 🚀 八、进阶方向

接下来建议继续学习哈希表进阶题目：

- 454. 四数相加 II  
- 383. 赎金信  
- 15. 三数之和  

重点体会哈希表在「计数」和「匹配查找」中的高效应用。
